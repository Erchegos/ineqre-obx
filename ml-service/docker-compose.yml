version: '3.8'

services:
  ml-service:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ml-models:/tmp/models
    restart: unless-stopped

volumes:
  ml-models:
