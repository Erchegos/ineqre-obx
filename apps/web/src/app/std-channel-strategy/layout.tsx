import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Std Deviation Channel Strategy | Mean-Reversion Backtest",
  description: "Standard deviation channel trading strategy for Oslo Børs. Linear regression channels with event-driven filters, optimized parameters, and full backtest with trade signals.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
